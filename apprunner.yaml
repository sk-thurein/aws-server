version: 1.0
runtime: python311  # Managed runtimes don't have a 'docker' keyword here
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: uvicorn main:app --host 0.0.0.0 --port 8080